"""SentinelHack agents package.

Each agent is a self-contained module that communicates through plain
Python function calls. The `black_box` module provides the incident
lifecycle and the `@black_box` decorator every agent uses for logging.
"""
